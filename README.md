# 🎯 Ikigai Finder (เครื่องมือค้นหาอิคิไกของตัวเอง)

Web Application สำหรับช่วยผู้ใช้ค้นหา **"อิคิไก"** (Ikigai - ความหมายของการมีชีวิตอยู่) ผ่านการทบทวนตัวเองด้วยคำถาม 4 มิติ ได้แก่ สิ่งที่รัก, สิ่งที่โลกต้องการ, สิ่งที่สร้างรายได้ และสิ่งที่ทำได้ดี โดยระบบจะนำคำตอบมาสร้างเป็น แผนภาพ Venn Diagram แบบ Interactive และสามารถดาวน์โหลดผลลัพธ์เป็นรูปภาพเก็บไว้ได้ พร้อมระบบจัดเก็บข้อมูลสถิติลง Google Sheets แบบไม่ต้องพึ่งพาเซิร์ฟเวอร์ (Serverless)

---

## ✨ ฟีเจอร์หลัก (Features)

* **Interactive Venn Diagram:** แผนภาพวงกลมจะอัปเดตข้อความแบบ Real-time ทันทีที่ผู้ใช้พิมพ์คำตอบ
* **Intersection Logic:** จับคู่และสรุปจุดซ้อนทับของตัวตน (Passion, Mission, Vocation, Profession)
* **Export to Image:** ผู้ใช้สามารถกดปุ่มเพื่อแคปเจอร์แผนภาพ Ikigai ของตัวเองและดาวน์โหลดเป็นไฟล์ `.png` ได้ทันที (ใช้ไลบรารี `html2canvas`)
* **Serverless Database:** บันทึกข้อมูลที่ผู้ใช้กรอกส่งตรงเข้า Google Sheets อัตโนมัติผ่าน Google Apps Script
* **Responsive Design:** รองรับการแสดงผลทั้งบนคอมพิวเตอร์และโทรศัพท์มือถือ (ออกแบบด้วย Tailwind CSS)

---

## 🛠 เทคโนโลยีที่ใช้ (Tech Stack)

* **Frontend:** HTML5, Vanilla JavaScript
* **CSS Framework:** Tailwind CSS (via CDN)
* **Typography:** Google Fonts (Kanit, Mali)
* **Library:** `html2canvas` (สำหรับการเซฟรูปภาพ)
* **Backend / Database:** Google Apps Script (GAS) + Google Sheets

---

## 🚀 วิธีการติดตั้งและใช้งาน (Getting Started)

โปรเจกต์นี้ทำงานฝั่ง Frontend เป็นหลัก แต่จำเป็นต้องตั้งค่า Google Sheets เพื่อใช้เป็นฐานข้อมูลสำหรับเก็บสถิติ

### 1. การตั้งค่า Google Sheets (Database)
1. สร้าง **Google Sheets** ไฟล์ใหม่บน Google Drive
2. ในแถวแรก (Row 1) ให้สร้างหัวข้อคอลัมน์ A ถึง E ดังนี้: `Timestamp` | `สิ่งที่รัก` | `โลกต้องการ` | `สร้างรายได้` | `ทำได้ดี`
3. ไปที่เมนู **ส่วนขยาย (Extensions)** > **Apps Script**
4. ลบโค้ดเดิมออกให้หมด แล้ววางโค้ดนี้ลงไป:(```javascript ด้านล่าง)
5. กดปุ่ม บันทึก และกด การทำให้ใช้งานได้ (Deploy) > การทำให้ใช้งานได้รายการใหม่ (New deployment)
6. เลือกประเภทเป็น เว็บแอป (Web app) และตั้งค่า สิทธิ์การเข้าถึง (Who has access) ให้เป็น "ทุกคน (Anyone)"
7. กด ทำให้ใช้งานได้ (Deploy) และ คัดลอก URL ของเว็บแอป เก็บไว้

2. การตั้งค่า Frontend
Clone repository นี้ลงเครื่อง:

Bash

git clone [https://github.com/nhuying/ikigai.git](https://github.com/nhuying/ikigai.git)
เปิดไฟล์ index.html ด้วย Text Editor

ค้นหาบรรทัดที่เขียนว่า const GOOGLE_SCRIPT_URL = "...";

นำ URL ที่ได้จากขั้นตอนที่ 1.7 มาวางแทนที่

สามารถนำไฟล์ index.html ไปอัปโหลดขึ้นโฮสติ้งฟรีอย่าง GitHub Pages หรือ Vercel เพื่อเปิดใช้งานออนไลน์ได้ทันที

📝 วิธีการใช้งานสำหรับผู้ใช้ทั่วไป
เข้าสู่เว็บไซต์

พิมพ์คำตอบในช่องเช็กลิสต์ทั้ง 4 ข้อให้ครบถ้วน

ดูแผนภาพวงกลมตรงกลางที่จะเติมเต็มและสว่างขึ้นเมื่อกรอกครบ

กดปุ่ม "📸 บันทึกรูปภาพ Ikigai ของฉัน" 5. ระบบจะดาวน์โหลดไฟล์รูปภาพลงเครื่อง และส่งข้อมูลสถิติไปยัง Google Sheets โดยอัตโนมัติ

🧑‍💻 การสร้างโดย (Created By)
Developer: nhuying

GitHub Profile: https://github.com/nhuying

Live Website: https://nhuying.github.io/ikigai/

โปรเจกต์นี้สร้างขึ้นเพื่อเป็นเครื่องมือช่วยให้ทุกคนได้มีพื้นที่ทบทวนจิตใจ ค้นพบจุดสมดุล และเห็นคุณค่าในทุกๆ วันของการใช้ชีวิต


```javascript
function doPost(e) { 
  try { 
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet(); 
    var data = JSON.parse(e.postData.contents); 
    var timestamp = new Date();

    sheet.appendRow([timestamp, data.love, data.world, data.paid, data.good]);

    return ContentService.createTextOutput(JSON.stringify({"result":"success"}))
      .setMimeType(ContentService.MimeType.JSON);   
  } catch(error) {
    return ContentService.createTextOutput(JSON.stringify({"result":"error", "error": error.message}))
      .setMimeType(ContentService.MimeType.JSON);   
  }
}








